{
  "name": "dentalcare-pro",
  "version": "1.0.0",
  "description": "Unified Dental Appointment System",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "dev": "cd backend && npm run dev",
    "install-all": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
